apiVersion: apps/v1
kind: Deployment
metadata:
 name: orderer-deployment-proposer
spec:
 replicas: 1
 selector:
  matchLabels:
    app: orderer-proposer
 template:
   metadata:
     labels:
       app: orderer-proposer
   spec:
     containers:
     - name: orderer
       imagePullPolicy: IfNotPresent
       image: orderer:v1
       envFrom:
        - configMapRef:
            name: orderer-configmap 
       args: ["-nodeId=$(NODEID_1)", "-configFilePath=$(CONFIG_FILE_PATH)"]
       ports:
       - containerPort: 3000
       
