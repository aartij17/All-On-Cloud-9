apiVersion: apps/v1
kind: Deployment
metadata:
 name: orderer-deployment-replica
spec:
 replicas: 1
 selector:
  matchLabels:
    app: orderer-replica
 template:
   metadata:
     labels:
       app: orderer-replica
   spec:
     containers:
     - name: orderer
       imagePullPolicy: IfNotPresent
       image: orderer:v1
       envFrom:
        - configMapRef:
            name: orderer-configmap 
       args: ["-nodeId=$(NODEID_5)", "-configFilePath=$(CONFIG_FILE_PATH)"]
       ports:
       - containerPort: 3000
       
