package blockchain

import (
	"All-On-Cloud-9/server/nodes"
	"context"
)

type Node = nodes.Node

func (n *nodes.Node) AddNewBlock(ctx context.Context, newBlock Block, toBlockId string) {
	n := nodes.Node{

	}
}
